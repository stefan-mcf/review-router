# inbox-triage-router

Deterministic inbox triage for sales, support, billing, or manual review.

Fixture-safe by default. Run via `review-router run inbox-triage-router --fixture templates/inbox-triage-router/fixtures/sample-input.json`.
