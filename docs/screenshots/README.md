# Screenshot Evidence

All screenshots in this directory are local, synthetic, and public-safe. They are generated by `PYTHONPATH=src python3.11 scripts/generate_screenshots.py` after the CLI/API/runtime paths are real and verified.

The generator follows the same dark proof-panel style used in the stronger sibling proof repos, but now each panel includes concrete command excerpts, API fields, queue packets, mapping notes, or verification output instead of placeholder text.

| File | What it proves |
|---|---|
| `01-flow-overview.png` | End-to-end review-router control flow: input -> deterministic AI -> review gate -> route -> handoff. |
| `02-cli-proof.png` | CLI can list templates, validate a workflow, and run a real synthetic inbox fixture. |
| `03-api-openapi.png` | Local API exposes fixture-safe `/health`, `/templates`, `/validate`, `/run`, `/runs/{id}`, and `/queue/resolve` surfaces. |
| `04-template-output-proof.png` | Lead enrichment fixture produces deterministic routing, confidence, and destination output. |
| `05-review-queue-proof.png` | Creative workflows and uncertain routes stop in the file-backed review queue before any live action. |
| `06-low-code-mapping-proof.png` | n8n, Make, and Zapier mappings plus credential boundaries are documented and reviewable. |
| `07-quality-gates.png` | Pytest, ruff, mypy, template validation, and public readiness checks passed when evidence was captured. |
| `08-safety-boundary.png` | `.env.example` and runtime metadata keep the proof fixture-safe with empty placeholders only. |

Boundary: `fixture_safe=true`, `live_services_used=false`, `synthetic_data_only=true`.
