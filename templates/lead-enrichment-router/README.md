# lead-enrichment-router

Deterministic lead enrichment and routing workflow with manual research fallback.

Fixture-safe by default. Run via `review-router run lead-enrichment-router --fixture templates/lead-enrichment-router/fixtures/sample-input.json`.
